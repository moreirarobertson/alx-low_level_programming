#include "main.h"

/**
 * _abs - A function that returns  a non-negative value of an integer
 * @n: input value
 * Return: the non-negative value of integer
 */
int _abs(int n)
{
	if (n >= 0) 
	{
		return (n); 
	}
	return (-n); 
}
