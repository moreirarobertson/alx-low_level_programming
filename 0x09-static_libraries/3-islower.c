#include "main.h"
/**
 * _islower - checks if a character is lower case
 * @c : input character
 * Return: 1 if c is lowercase or  0 if otherwise
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
