#include "main.h"
/** _isdigit - a function that looks for a digit (0-9)
 * @c - the integer to be checked
 *
 * return: 1 if c is a digit and 0 if is a non-digit
 */
int _isdigit(int c)
{
   int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
}
